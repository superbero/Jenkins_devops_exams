apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-helm-staging.fullname" . }}
  labels:
    {{- include "jenkins-helm-staging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-helm-staging.selectorLabels" . | nindent 4 }}
