apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-helm-QA.fullname" . }}
  labels:
    {{- include "jenkins-helm-QA.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-helm-QA.selectorLabels" . | nindent 4 }}
