apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-data-jenkins
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  hostPath:
    path:  /Users/admin/data
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: standard
  